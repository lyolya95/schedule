import React from 'react';
import ReactDOM from 'react-dom';
import {MentorFilters} from "./components/MentorFilters/MentorFilters";

ReactDOM.render(
  <React.StrictMode>
    <MentorFilters/>
  </React.StrictMode>,
  document.getElementById('root')
);
